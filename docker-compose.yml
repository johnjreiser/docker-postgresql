version: '3.2'
services:
    db:
        build: .
        ports:
            - "5432:5432"
        volumes:
            - "/opt/local/pgsql/10/data:/var/lib/pgsql/10/data"
        env_file:
            - env_file.txt

